#include <array>
#include <iostream>
#include <string>

static const int numrows = 10;

typedef std::array<std::array<std::string, numrows>, numrows> board;
typedef std::array<std::array<int, numrows>, numrows>         pboard;

int main() {
    return 0;
}